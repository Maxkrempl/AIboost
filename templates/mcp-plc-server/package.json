{
  "name": "mcp-plc-server",
  "version": "1.0.0",
  "description": "MCP Server za industrijsko avtomatizacijo — poveže AI s PLC krmilniki",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "simulator": "python3 simulator/plc_simulator.py",
    "demo": "echo '1. Run: npm run simulator (in terminal 1)' && echo '2. Run: npm run dev (in terminal 2)' && echo '3. Connect AI to MCP server'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.25.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  }
}
